# Golang deployment using serverless framework

This is a debian based image that sets up the golang environment for development.

The image has the following setup:

* go
* node
* awscli
* serverless
